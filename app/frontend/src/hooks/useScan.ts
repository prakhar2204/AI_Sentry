/**
 * AI-SENTRY -- useScan Hook
 * hooks/useScan.ts
 *
 * Convenience wrapper around ScanContext.
 * Provides typed state + dispatch to any component.
 */

export { useScanContext } from "../store/ScanContext";
